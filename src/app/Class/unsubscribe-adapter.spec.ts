import { UnsubscribeAdapter } from './unsubscribe-adapter';

describe('UnsubscribeAdapter', () => {
  it('should create an instance', () => {
    expect(new UnsubscribeAdapter()).toBeTruthy();
  });
});
